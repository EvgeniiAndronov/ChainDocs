# Prometheus конфигурация для ChainDocs
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'chaindocs-server'
    static_configs:
      - targets: ['chaindocs-server:8080']
    metrics_path: '/metrics'
    scrape_interval: 10s
